import React from 'react'
import RandomColour from './RandomColour';

const PortfolioItemWrapper = ({ children }) =>
<React.Fragment>{ children }</React.Fragment>

export default RandomColour(PortfolioItemWrapper)